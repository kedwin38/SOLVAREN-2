{
  "name": "solvaren",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "version": "1.0.0",
  "description": "SOLVAREN Payment Solutions — enterprise disbursement control plane for Safaricom M-PESA Daraja B2C. Move money with certainty.",
  "scripts": {
    "build:packages": "pnpm --filter @solvaren/core build && pnpm --filter @solvaren/daraja build && pnpm --filter @solvaren/storage build",
    "build": "pnpm build:packages && pnpm --filter @solvaren/api build && pnpm --filter @solvaren/web build",
    "typecheck": "pnpm build:packages && pnpm -r --if-present typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:migrate": "node scripts/migrate.mjs",
    "db:up": "node scripts/dev-postgres.mjs up",
    "db:down": "node scripts/dev-postgres.mjs down",
    "db:test": "node scripts/db-test.mjs",
    "db:generate-seed": "node scripts/generate-failure-seed.mjs",
    "check:invariants": "node scripts/check-invariants.mjs",
    "ui:check": "node scripts/ui-check.mjs",
    "create-user": "node scripts/create-user.mjs",
    "restore": "node scripts/restore-snapshot.mjs",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:invariants && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=22"
  }
}
